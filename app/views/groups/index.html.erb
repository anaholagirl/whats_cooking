<h1>Category Listings:</h1>

<% @groups.each do |group| %>
  <li><%= link_to group.name, user_group_path(group) %></li>
<% end %>

<%= link_to "Create a new Category", new_user_group_path, :class => 'btn btn-primary' %>

<%= link_to "Homepage", root_path, :class => 'btn btn-success' %>
